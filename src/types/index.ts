export type NaverTokenType = {
  access_token: string;
  refresh_token: string;
  token_type: string;
  expires_in: string;
};

export type NaverProfileType = {
  id: string;
  profile_image: string;
  name: string;
};
